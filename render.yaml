services:
  - type: web
    name: tailored-admin-dashboard-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGODB_URI
        value: your-mongodb-uri-here
